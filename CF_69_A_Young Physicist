#include<iostream>
using namespace std;
int main()
{
    int n,x,y,z,p=1,a=0,b=0,c=0;
    cin>>n;
    while(p<=n)
    {
        cin>>x>>y>>z;
        a=a+x;
        b=b+y;
        c=c+z;
        ++p;
    }
    if (a==0&&b==0&&c==0) cout<<"YES";
    else cout<<"NO";
    return 0;
}
